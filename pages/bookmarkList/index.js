import ActivityList from "@"

export default function FavoritesActivitiesPage({title,
    categories,
    area,
    country,
    description,
    imageUrl}) {
        const favorites = title.filter((title) => 
        )
    

    return ();
}